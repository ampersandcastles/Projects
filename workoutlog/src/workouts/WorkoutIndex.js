import React from "react";
import { Container, Row, Col } from "reactstrap";

const WorkoutIndex = (props) => {
  const [workouts, setWorkouts] = useState([]);

  return <div>Workout Index</div>;
};

export default WorkoutIndex;
